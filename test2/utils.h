//
//  utils.hpp
//  test2
//
//  Created by Lang Tian on 2021/7/30.
//

#ifndef utils_hpp
#define utils_hpp

#include <vector>
#include <stdio.h>

#include <IOKit/hid/IOHIDLib.h>
#include <IOKit/IOKitLib.h>


/*
template<typename T>
std::vector<T> cfset_to_vector(CFSetRef cf_set)
{
    size_t size = CFSetGetCount(cf_set);
    std::vector<T> array(size);
    CFSetGetValues(cf_set, (const void **)array.data());
    return array;
}
 */

#endif /* utils_hpp */
